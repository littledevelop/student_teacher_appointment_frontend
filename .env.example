# Backend API URL (no trailing slash). Required for API calls.
REACT_APP_API_URL=http://localhost:5000

# Optional: same as above (some setups use this name)
# REACT_APP_Backend_URL=http://localhost:5000

# Firebase (if using notifications / Firestore) - copy from Firebase console
# apiKey=
# authDomain=
# projectId=
# storageBucket=
# messagingSenderId=
# appId=
